ALGORITHME Chaine

var
 x , y , z:INTEGER;
 ch:CHAR;

BEGIN
x:=0;
y:=0;
z:=0;
REPEAT
read (ch);

if((ch='.')or(ch=" "))then
{
CONTINUE
}
     x:=x+1;
if (ch=" ") then
y:=y+1;
endif
if ((ch='A')or((ch='I')or((ch='O')or((ch='E')or(U(ch='U')or((ch='Y')then
z:=z+1;
endif
UNTIL (ch='.'))
whrite("le nombre de caracteres dans cette chaine est : " ,x+1);
whrite("le nombre de mots dans cette chaine est : " ,y+1);
whrite("le nombre de voyelles dans cette chaine est : " ,z);
END